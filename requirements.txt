# Pinned exactly, on purpose.
#
# The np.trapz shim in dcc_garch.py existed because NumPy versions had drifted
# apart across the three machines this course is prepared on.  Loose bounds
# (>=) let that happen again; every machine and Streamlit Community Cloud must
# resolve to the same versions.
#
# Verified on Python 3.14.5 (Windows).  On Streamlit Community Cloud set the
# app's Python version to 3.13 or newer, otherwise these wheels do not resolve.
# To refresh: upgrade locally, run `pytest python/tests`, then re-pin.

streamlit==1.58.0
pandas==3.0.3
numpy==2.4.6
scipy==1.17.1
plotly==6.9.0
statsmodels==0.14.6
scikit-learn==1.9.0
arch==8.0.0
numba==0.65.1
matplotlib==3.10.9

# External data input (Task D)
yfinance==1.4.1
openpyxl==3.1.5      # .xlsx uploads
pyarrow==24.0.0      # .parquet uploads
