[pytest]
testpaths = tests
markers =
    slow: long-running acceptance checks (bootstrap, N=200 fit)
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
    ignore::RuntimeWarning
